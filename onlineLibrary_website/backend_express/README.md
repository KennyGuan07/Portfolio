# Express Backend 

## Project Setup

```sh
npm install
```

### Start the Server

```sh
npm start
```

### Database Configuration

Ensure MongoDB is running locally or update `MONGODB_URI` in `utils/db.js`